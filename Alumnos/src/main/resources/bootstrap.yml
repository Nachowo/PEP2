spring:
  devtools:
    restart:
      aditional-paths: src/main/java/com/example/demo
  application:
    name: alumno-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8091
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/alumnos
    username: postgres
    password: 64959
  jpa:
    show-sql: true
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
      ddl-auto: update
